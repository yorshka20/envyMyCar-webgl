export function render(canvas: HTMLCanvasElement) {
  //

  setupWebGL();
  setupWhatToDraw();
  setupHowToDraw();
  draw();
}

function setupWebGL() {}

function setupWhatToDraw() {}

function setupHowToDraw() {}

function draw() {}
